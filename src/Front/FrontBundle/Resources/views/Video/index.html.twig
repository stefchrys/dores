{% extends "::base.html.twig" %}



{% block contenu %}

 <!--Contenu principal @media>1200px-->
 
            <div class="row visible-lg">
                 <div class="panel panel-default col-sm-4 col-md-4">
                    <div class="panel-heading panel-heading-custom">
                        <h3 class="panel-title">XIX eme</h3>
                    </div>

                    <div class="panel-body panel-body-custom">
                    	{% for c1 in cat1 %}
                        <video src="{{ asset('video/'~ c1.source) }}" width="100%" controls preload="none" ></video>
                        {% endfor %}
                    </div>
                           
                </div>
                 <div class="panel panel-default col-sm-4 col-md-4">
                    <div class="panel-heading panel-heading-custom">
                        <h3 class="panel-title">Contemporain</h3>
                    </div>

                    <div class="panel-body panel-body-custom">
                        {% for c2 in cat2 %}
                        <video src="{{ asset('video/'~ c2.source) }}" width="100%" controls preload="none" ></video>
                        {% endfor %}
                    </div>
                           
                </div>
                 <div class="panel panel-default col-sm-4 col-md-4">
                    <div class="panel-heading panel-heading-custom">
                        <h3 class="panel-title">Essais</h3>
                    </div>

                    <div class="panel-body panel-body-custom">
                        {% for c3 in cat3 %}
                        <video src="{{ asset('video/'~ c3.source) }}" width="100%" controls preload="none" ></video>
                        {% endfor %}
                    </div>
                           
                </div>  
                <hr>
            </div> 
            <!--fin contenu principal @media>1200px-->

            <!--Contenu principal @media >768 && <1200 px-->
            <div class="row visible-md visible-sm">
                <div class="panel panel-default col-sm-6 col-md-6">
                    <div class="panel-heading panel-heading-custom">
                        <h3 class="panel-title">XIX eme</h3>
                    </div>
                    <div class="panel-body panel-body-custom">
                        {% for c1 in cat1 %}
                        <video src="{{ asset('video/'~ c1.source) }}" width="100%" controls preload="none" ></video>
                        {% endfor %} 
                    </div>                          
                </div>

                <div class="panel panel-default col-sm-6 col-md-6">
                    <div class="panel-heading panel-heading-custom">
                        <h3 class="panel-title">Contemporain</h3>
                    </div>
                    <div class="panel-body panel-body-custom">
                        {% for c2 in cat2 %}
                        <video src="{{ asset('video/'~ c2.source) }}" width="100%" controls preload="none" ></video>
                        {% endfor %} 
                    </div>                          
                </div>
                <hr> 
            </div> 
            <!--fin contenu principal @media >768 && <1200 px-->

             <!--Contenu principal @media <768 px-->
            <div class="row visible-xs">
                 <div class="panel panel-default col-sm-6 col-md-6">
                    <div class="panel-heading panel-heading-custom">
                        <h3 class="panel-title">XIX eme</h3>
                    </div>
                    <div class="panel-body panel-body-custom">
                        {% for c1 in cat1 %}
                        <video src="{{ asset('video/'~ c1.source) }}" width="100%" controls preload="none" ></video>
                        {% endfor %}  
                    </div>                          
                </div>

                <div class="panel panel-default col-sm-6 col-md-6">
                    <div class="panel-heading panel-heading-custom">
                        <h3 class="panel-title">Contemporain</h3>
                    </div>
                    <div class="panel-body panel-body-custom">
                        {% for c2 in cat2 %}
                        <video src="{{ asset('video/'~ c2.source) }}" width="100%" controls preload="none"  ></video>
                        {% endfor %} 
                    </div>                          
                </div>
                <hr>
            </div>
            <!--fin contenu principal @media <768-->

{% endblock %}
