{% extends "::base.html.twig" %}



{% block contenu %}
 <!--Contenu principal @media>1200px-->
            <div class="row "> 
            {% for essai in essais %}              
                <div class="panel panel-default col-sm-6 col-md-4 col-lg-3">
                     <div class="media espace-livres "> 
                <a class="pull-left" <a href="{{ path('front_livre', { 'id': essai.id }) }}">
                <img width="100" height="154" class="media-object" src="{{ asset('img/collection/'~ essai.image) }}" alt="ESSAIS"> 
                </a> 
                <div class="media-body"> 
                    <h4 class="media-heading">{{ essai.auteur }}</h4> 
                    <p>{{ essai.titre }}</p> 
                    
                    
                    <p><font color="#FF0000">{{ essai.prix }}€</font></p>
                    
                </div> 
            </div>
                </div>
            {% endfor %}                
                <hr>
            </div> 
            <!--fin contenu principal @media>1200px-->
{% endblock %}
